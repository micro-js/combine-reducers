{
  "name": "@f/combine-reducers",
  "description": "Combines an object of reducers",
  "repository": "git://github.com/micro-js/combine-reducers.git",
  "version": "1.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/clone-shallow": "^1.0.0",
    "@f/compose-reducers": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
