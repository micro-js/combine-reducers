{
  "name": "@micro-js/combine-reducers",
  "description": "Combines an object of reducers",
  "repository": "git://github.com/micro-js/combine-reducers.git",
  "version": "1.0.2",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/clone-shallow": "^1.0.0",
    "@micro-js/compose-reducers": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
